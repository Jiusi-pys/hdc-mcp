{
  "name": "wsl-win-mcp-server",
  "version": "0.1.0",
  "description": "MCP server that runs inside WSL and bridges to Windows executables (powershell.exe / hdc.exe).",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^0.1.2",
    "zod": "^4.24.0"
  }
}
